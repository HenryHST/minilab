apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bookstack-stadthagen-dev
  namespace: bookstack
spec:
  secretName: bookstack-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - book.stadthagen.dev
