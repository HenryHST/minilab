# Populated by Ansible k3s_secrets from SecretSpec GRAFANA_OAUTH_CLIENT_SECRET.
# See Infra_LAB ansible/playbooks/k3s_cluster/secrets/README.md
#
# kubectl create secret generic grafana-oauth -n grafana \
#   --from-literal=client-id=grafana \
#   --from-literal=client-secret=CHANGE_ME
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: grafana-oauth
# type: Opaque
# stringData:
#   client-id: grafana
#   client-secret: CHANGE_ME
