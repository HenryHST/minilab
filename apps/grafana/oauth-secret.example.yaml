# Optional Authentik OAuth — create manually in namespace grafana, then uncomment
# env / envValueFrom in values.yaml (see pi3cl compose/grafana.yaml).
#
# kubectl create secret generic grafana-oauth -n grafana \
#   --from-literal=client-id=grafana \
#   --from-literal=client-secret=CHANGE_ME
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: grafana-oauth
# type: Opaque
# stringData:
#   client-id: grafana
#   client-secret: CHANGE_ME
