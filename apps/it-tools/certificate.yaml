apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: it-tools-stadthagen-dev
  namespace: it-tools
spec:
  secretName: it-tools-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - it-tools.stadthagen.dev
