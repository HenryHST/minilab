# Populated by Ansible k3s_secrets from SecretSpec GRAFANA_OAUTH_CLIENT_SECRET.
# Namespace: monitoring (was grafana before kube-prometheus-stack migration).
#
# kubectl create secret generic grafana-oauth -n monitoring \
#   --from-literal=client-id=grafana \
#   --from-literal=client-secret=CHANGE_ME
#
# Optional Hetzner Cloud datasource:
# kubectl create secret generic grafana-hcloud -n monitoring \
#   --from-literal=token=CHANGE_ME
