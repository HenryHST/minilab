apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn-stadthagen-dev
spec:
  secretName: longhorn-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - longhorn.stadthagen.dev
