apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: termix-data
  annotations:
    longhorn.io/volume-replica-count: "1"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
