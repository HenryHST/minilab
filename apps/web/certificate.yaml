apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-stadthagen-dev
  namespace: homepage
spec:
  secretName: web-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - web.stadthagen.dev
