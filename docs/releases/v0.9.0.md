## Summary

Authentik als Cluster-IdP unter `apps/authentik` inkl. **Day-0 Blueprint** für den registry-ui ForwardAuth-Outpost (`ak-outpost-registry-ui`). Application trackt `main`.

Begleit-Release Plattform: **[Infra_LAB v1.7.0](https://github.com/HenryHST/Infra_LAB/releases/tag/v1.7.0)** (ADR-0017 reconcile / `authentik-bootstrap`).

## Added

- `apps/authentik` — Helm 2026.8, PostgreSQL (Longhorn), Media PVC + NFS CronJob, IngressRoute `idp.stadthagen.dev`
- Day-0 Blueprints — `BLUEPRINTS.md`, ConfigMap + `blueprints.configMaps`

## Changed

- Application `authentik` → `targetRevision: main`
- registry-ui ForwardAuth wieder aktiv (#56)

## Ops

Nach Sync: `kubectl -n authentik get svc | grep registry-ui` → Outpost Ready, dann ForwardAuth nutzbar. IdP-Reconcile von Infra_LAB: `ansible-playbook site.yaml --tags authentik-bootstrap`.

See [CHANGELOG.md](https://github.com/HenryHST/minilab/blob/v0.9.0/CHANGELOG.md) for details.

**Full Changelog**: https://github.com/HenryHST/minilab/compare/v0.8.0...v0.9.0
