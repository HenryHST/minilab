apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: alloy
resources:
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: alloy-config
    files:
      - config.alloy
generatorOptions:
  disableNameSuffixHash: true
