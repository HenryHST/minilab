apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: certmanager
resources:
  - clusterissuer.yaml
# Helm charts: cert-manager + webhook via native Argo CD Helm (ApplicationSet infra).
