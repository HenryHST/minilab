apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - storageclass.yaml
  - pvc.yaml
  - certificate.yaml
  - ingressroute.yaml
  - networkpolicy.yaml
# Helm chart is rendered via native Argo CD Helm source (ApplicationSet infra).
