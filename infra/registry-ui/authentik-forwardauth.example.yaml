# Example / reference: Traefik ForwardAuth → Authentik Outpost for registry-ui.stadthagen.dev.
# Live Middleware is applied from manifests/middleware-authentik.yaml.
#
# Authentik setup (still manual):
# 1. Create Authentik Proxy Provider + Outpost for Host registry-ui.stadthagen.dev
# 2. Ensure Service name matches forwardAuth.address (default ak-outpost-registry-ui)
#
# apiVersion: traefik.io/v1alpha1
# kind: Middleware
# metadata:
#   name: registry-ui-authentik
#   namespace: registry-ui
#   labels:
#     app.kubernetes.io/name: docker-registry-ui
#     app.kubernetes.io/part-of: registry-ui
# spec:
#   forwardAuth:
#     address: http://ak-outpost-registry-ui.authentik.svc.cluster.local:9000/outpost.goauthentik.io/auth/traefik
#     trustForwardHeader: true
#     authResponseHeaders:
#       - X-authentik-username
#       - X-authentik-groups
#       - X-authentik-email
#       - X-authentik-name
#       - X-authentik-uid
