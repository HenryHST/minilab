# Example: Traefik ForwardAuth → Authentik Outpost for registry-ui.stadthagen.dev.
# Not applied by Argo CD (*.example.yaml). Steps:
# 1. Create Authentik Proxy Provider + Outpost for Host registry-ui.stadthagen.dev
# 2. Copy this file to manifests/middleware-authentik.yaml and adjust address
# 3. Uncomment middlewares on IngressRoute registry-ui
# 4. Uncomment Authentik egress in manifests/networkpolicy.yaml
#
# apiVersion: traefik.io/v1alpha1
# kind: Middleware
# metadata:
#   name: registry-ui-authentik
#   namespace: registry-ui
#   labels:
#     app.kubernetes.io/name: docker-registry-ui
#     app.kubernetes.io/part-of: registry-ui
# spec:
#   forwardAuth:
#     address: http://ak-outpost-registry-ui.authentik.svc.cluster.local:9000/outpost.goauthentik.io/auth/traefik
#     trustForwardHeader: true
#     authResponseHeaders:
#       - X-authentik-username
#       - X-authentik-groups
#       - X-authentik-email
#       - X-authentik-name
#       - X-authentik-uid
