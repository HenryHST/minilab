# Rancher system-upgrade-controller v0.20.1 (controller + CRDs only; no Plan CRs yet).
# Upstream manifests vendored from:
# https://github.com/rancher/system-upgrade-controller/releases/tag/v0.20.1
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - crd.yaml
  - system-upgrade-controller.yaml
